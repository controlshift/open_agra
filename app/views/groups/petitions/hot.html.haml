- content_for :sidebar do
  = render partial: 'groups/manage/group_admin_sidebar'

= title "#{@group.title} Hot Petitions", class: 'left'
.clear


= render partial: 'admin/petitions/petition_hot_table'


