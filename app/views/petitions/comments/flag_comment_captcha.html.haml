= form_tag flag_petition_comment_path(@petition, @comment), :remote => true, :method => :put, :id => 'flag-captcha-form' do
  = show_simple_captcha label: "Please type the text from the image."
  .actions
    = button_tag 'Submit', id: "captcha-button", class: "btn btn-primary mb10 mr5"